server {
    listen       8081;
    server_name  _;

    # The lines below are handled by entrypoint.sh.
    # Do not delete it.
    #listen 443 ssl;
    #ssl_certificate @cert@;
    #ssl_certificate_key @certkey@;

    proxy_redirect          off;
    proxy_set_header        Host            $host;
    proxy_set_header        X-Real-IP       $remote_addr;
    proxy_set_header        X-Forwarded-For $proxy_add_x_forwarded_for;
    client_body_buffer_size 128k;
    proxy_connect_timeout   90;
    proxy_send_timeout      90;
    proxy_read_timeout      300;
    proxy_buffers           32 4k;

	set $PATH /Users/shixizheng/workspace/digitalbrain/label/label-studio;

    location /label/ {
        absolute_redirect off;
        proxy_pass http://localhost:8080/;
        add_header 'Access-Control-Allow-Origin' '*';
        client_max_body_size 200m;   # adjust to taste
    }

    location /label/data/upload {
        alias $PATH/data/media/upload/;
    }

    location /label/data/avatars {
        alias $PATH/data/media/avatars;
    }

    location /label/static {
        alias $PATH/label_studio/core/static/;
        add_header 'Access-Control-Allow-Origin' '*';
    }

    location /label/label-studio-frontend {
        alias $PATH/label_studio/frontend/dist/lsf;
    }

    location /label/react-app {
        alias $PATH/label_studio/frontend/dist/react-app;
    }

    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}
